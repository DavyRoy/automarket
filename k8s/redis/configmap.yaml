apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: automarket
data:
  REDIS_HOST: "redis-svc"
  REDIS_PORT: "6379"
  redis.conf: |
    maxmemory 128mb
    maxmemory-policy allkeys-lru
    appendonly yes